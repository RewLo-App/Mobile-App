# Production uses the API on the same public origin.
VITE_API_URL=/api/v1

# For local development, override the value in .env.local when the API runs
# separately. Do not put a Replit deployment domain in this file.
# VITE_API_URL=http://localhost:3001/api/v1
