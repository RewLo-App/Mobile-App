{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:merchant": "pnpm --filter @workspace/merchant-dashboard build",
    "build:api": "pnpm --filter @workspace/api-server build",
    "build:mobile": "pnpm --filter @workspace/mobile build",
    "build:deployment": "pnpm run build:merchant && pnpm run build:api && pnpm run build:mobile",
    "start:deployment": "node artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "playwright": "^1.62.0",
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "stripe": "^22.3.2",
    "stripe-replit-sync": "^1.0.0"
  }
}
